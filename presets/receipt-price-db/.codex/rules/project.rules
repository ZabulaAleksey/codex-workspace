# Локальная политика проекта. Глобальные rules продолжают действовать.
prefix_rule(
    pattern = ["git", "push"],
    decision = "prompt",
    justification = "Внешняя операция записи проекта требует подтверждения.",
)
