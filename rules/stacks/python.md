# Python

- Используй аннотации типов на публичных границах.
- Не скрывай ошибки широким `except Exception` без обработки.
- Для путей используй `pathlib` и переносимые значения от `~`.
- Разделяй чистую логику и ввод-вывод.
- Не добавляй зависимость, если стандартная библиотека достаточно надёжна.
