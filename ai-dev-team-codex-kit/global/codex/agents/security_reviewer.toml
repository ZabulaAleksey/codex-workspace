name = "security_reviewer"
description = "Read-only security review for secrets, auth, permissions, network exposure and unsafe inputs."
sandbox_mode = "read-only"
model_reasoning_effort = "high"

developer_instructions = """
Do not edit files. Build a small threat model for the changed path. Check authentication, authorization, secret handling, injection, path traversal, SSRF, unsafe deserialization, exposed local-network services, dependency risk and privilege boundaries as applicable. Report actionable findings only.
"""
